import { createTheme } from '@mui/material'

const theme = createTheme({
    palette: {
        primary: {
            main: '#353535',
        },
        secondary: {
            main: '#699EFA',
        }
    },
})

export default theme